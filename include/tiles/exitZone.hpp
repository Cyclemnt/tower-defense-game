#ifndef EXIT_ZONE_HPP
#define EXIT_ZONE_HPP

#include "tile.hpp"

class ExitZone: private Tile {
private:
    /* data */
    
public:
    ExitZone(/* args */);
    ~ExitZone();
};

#endif // EXIT_ZONE_HPP
