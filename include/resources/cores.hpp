#ifndef CORES_HPP
#define CORES_HPP

class Cores {
private:
    // Where are they?
    int present;
    int away;
    int stolen;

public:
    Cores(/* args */);
    ~Cores();
};

#endif // CORES_HPP
